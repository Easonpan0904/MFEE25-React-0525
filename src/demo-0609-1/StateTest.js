import React, { useState } from 'react';

function StateTest() {
  const [stateA, setStateA] = useState({ a: '' });
  const [stateB, setStateB] = useState({ b: '' });
  const [stateC, setStateC] = useState({ c: '' });
  return <></>;
}

export default StateTest;
